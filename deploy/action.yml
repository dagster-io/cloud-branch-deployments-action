# action.yml
name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  pr:
    required: false
  image_tag:
    required: false
  organization_id:
    required: false
  location:
    required: true
runs:
  using: 'docker'
  image: '../Dockerfile'
  entrypoint: '/deploy.sh'
  args:
    - ${{ inputs.pr }}
